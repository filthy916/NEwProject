CyberDork — Core scaffold

This repository has been re-scaffolded to host the CyberDork core engine (ACRG + Nexus + PoC Mesh).

Next steps:
- Implement core engine: module interface, proof vault, ACRG skeleton
- Wire real API keys into .env (do NOT commit secrets)
- Run npm install and begin implementation
